{
  "name": "bbq_sauce_techjam (Python 3.13, CPU)",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.13-bookworm",
  "postCreateCommand": "pip install --upgrade pip && pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu && pip install -r requirements.txt && (python scripts/download_models.py || echo 'model vendoring skipped -- run: python scripts/download_models.py')",

  "forwardPorts": [8501],
  "portsAttributes": {
    "8501": { "label": "Streamlit UI", "onAutoForward": "openBrowser" }
  },

  "remoteEnv": {
    "PIP_DISABLE_PIP_VERSION_CHECK": "1",
    "PYTHONDONTWRITEBYTECODE": "1",
    "HF_HUB_OFFLINE": "1",
    "TRANSFORMERS_OFFLINE": "1",
    "COPILOT_DEVICE": "cpu"
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "python.testing.pytestEnabled": true,
        "python.testing.pytestArgs": ["tests"]
      }
    }
  }
}
